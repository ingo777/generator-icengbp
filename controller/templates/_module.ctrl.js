(function(module) {
    'use strict';

    module.controller('<%= capitalModuleName %>Controller', function () {
        var model = this;

        init();

        function init() {

        }
    });

}(angular.module("<%= fullModuleName %>")));
