(function (module) {
    'use strict';

    module.controller('<%= fullModuleName %>.<%= capitalModuleName %>Controller', function () {
        var vm = this;

        init();

        function init() {

        }
    });

}(angular.module('<%= fullModuleName %>')));
